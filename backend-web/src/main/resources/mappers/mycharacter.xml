<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ssafytask01.mapper.MyCharactermapper">
    <insert id="insertMyCharacter" parameterType="MycharacterDto">
        insert into mycharacter (myCharacterId, nickname, characterId, childrenId)
        values (#{myCharacterId},#{nickname},#{characterId},#{childrenId})
    </insert>

    <update id="updateMyCharacter" parameterType="MycharacterDto">
        update mycharacter
        set myCharacterID = #{myCharacterId},
            nickname = #{nickname},
            characterId = #{characterId}
        where childrenId = #{childrenId}
    </update>

    <delete id="deleteMyCharacter" parameterType="int">
    delete
    from mycharacter
    where characterId = #{characterId}
    </delete>

    <select id="selectMyCharacter" resultType="MycharacterDto">
        select myCharacterId, nickname, characterId, childrenId
        from mycharacter
        where childrenId = #{childrenId}
    </select>

</mapper>